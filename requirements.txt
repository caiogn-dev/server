# Django
Django>=5.2,<6.0
django-unfold>=0.40.0
djangorestframework>=3.14.0
django-cors-headers>=4.3.0
django-filter>=23.5
drf-spectacular>=0.27.0
drf-nested-routers

# WebSockets
channels>=4.0.0
channels-redis>=4.1.0
daphne>=4.0.0
websockets>=12.0,<14.0  # Pin to avoid transfer_data_task AttributeError

# Database
psycopg2-binary>=2.9.9

# Celery
celery>=5.3.4
# redis 8.x quebra o channels-redis: redis.exceptions.TimeoutError deixou de
# herdar de asyncio.TimeoutError, o loop do channel layer não captura e o
# consumer WS morre a cada ~30s com 1006 (visto em prod 18/jul).
redis>=5.0.1,<6
django-celery-beat>=2.5.0
flower>=2.0.1  # dashboard de monitoramento de filas/workers (serviço flower no compose)

# HTTP Client
requests>=2.31.0

# Security
cryptography>=41.0.7

# Production
gunicorn>=21.2.0
uvicorn[standard]>=0.27.0
whitenoise>=6.6.0

# Development
python-dotenv>=1.0.0

# Export
openpyxl>=3.1.2
xlsxwriter>=3.1.9

# PDF receipt generation (pure-Python, no system libs needed)
reportlab>=4.0.0

# Web Push notifications (VAPID)
pywebpush>=2.0.0

# Payments
mercadopago>=2.3.0

# Email
resend>=0.7.0

# Storage (AWS S3)
django-storages>=1.14.0
boto3>=1.34.0

# Image processing
Pillow>=10.0.0

# Telefone: DDI/DDD/validação de QUALQUER país (libphonenumber do Google).
# Substitui a heurística "sem 55 e até 11 dígitos → gruda 55", que transformava
# celular estrangeiro completo em brasileiro inexistente.
phonenumbers>=8.13

# Langchain & AI (Native - sem Langflow)
langchain>=0.3.0
langchain-openai>=0.2.0
langchain-community>=0.3.0
langchain-core>=0.3.0
langchain-anthropic>=0.1.0
anthropic>=0.40.0
openai>=1.0.0

# MCP (Model Context Protocol) — bot pipeline inspector
mcp>=1.0.0

# Google Drive API (Postado SaaS)
google-api-python-client>=2.100.0
google-auth>=2.20.0

# Testing
pytest>=7.0.0
pytest-django>=4.7.0

# Observabilidade (ativa só com SENTRY_DSN no env)
sentry-sdk>=2.0.0
